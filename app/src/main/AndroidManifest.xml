<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ramapps.mahabharatam.telugu" >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MSplashActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".HomeActivity"
            android:label="@string/parvam_menu_title"
            android:screenOrientation="portrait"
            />

        <activity
            android:name=".MChapterListActivity"
            android:label="@string/chapters_menu_title"
            android:screenOrientation="portrait"
            />

        <activity
            android:name=".MChapterReadActivity"
            android:label="@string/chapters_menu_title"
            android:screenOrientation="portrait"
            />

    </application>

</manifest>
